{
  "name": "supabase-edge-function",
  "version": "1.0.0",
  "description": "A Supabase edge function that sends a message to the general channel on our Slack when the feedback table is updated.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^1.29.0",
    "@slack/web-api": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}